# WellEarnedReviews: Project Handoff

**Updated 2026-07-13.** Since the overnight-session update below, all of this shipped and was verified: (a) the FIRST TASKS checklist, ALL SIX items, done 2026-07-12 ~10:15am PT (sends rows with Resend ids, contacts whole-row cleanup confirmed, SMS send-log test with Twilio SID, baselines backfilled booths 5.0/2 + entertainment 3.5/8 with joined_at, suppression skip verified, Phase 5 acceptance PASSED with screenshots); (b) passwordless customer login LIVE at /login plus the scoreboard referral program (new Customer login section below); (c) comp accounts + invite action in /admin for free testers (engine section); (d) the SEO gap build: 14 new pages including /free-reputation-report, audit recrawl to health 100, site copy standards, and a POSITIONING CHANGE, no-login absolutes removed sitewide, pitch is now "run it by text, or use the simple dashboard, your choice" (site section); (e) GBP agency-model setup runbook, proven live on Modern Roofers (GBP section); (f) Jul 13: HighLevel teardown (docs/2026-07-13-highlevel-teardown.md), per-client A2P client-SMS runbook (docs/2026-07-13-a2p-client-sms-runbook.md), and the CRM plan amended with Phase 6 ride-alongs + a Phase 7 GBP OAuth section (docs/2026-07-12-crm-plan.md). Latest site source: site/wer-site-2026-07-12p.zip. Operational gotchas live in project memory (wer_crm_build, wer_seo_gap, wer_customer_login, wer_gbp_setup); this doc stays canonical.
**PATH FORWARD (current, agreed through 2026-07-13):** (1) CRM Phase 6, now ~1.5 days: monthly report + archive + four ride-alongs (email.opened tracking, replied_at + mark-replied, industry benchmark line, minimal competitor tracking via businesses.competitor_place_ids); (2) Event Mode kiosk (~half day, spec ready); (3) Phase 7 GBP OAuth when Google approves (~Jul 19-22). Chris in parallel: Stripe live activation, WER GBP video verification, pilot #1 pick, Google OAuth client id for /login Sign-In (open item 10), referral terms on /terms before billing goes live (open item 11). Money dates: GBP API decision ~Jul 19-22; GatherUp $99 charge Jul 25 (reminder fires Jul 23, trig_016XBEsJSZyJPPXzXpiNqA9w); HighLevel $497 charge ~Aug 10 (reminder fires Aug 3, trig_01Te5PwYCYXvbbzsLo2szWza).
**Prior update, 2026-07-12 (overnight session, 1am-9:45am PT).** Shipped and verified: CRM Phase 4 (review detection: exit rule, low-review alerts, 180-day cooldown) and Phase 5 (portal v1 scoreboard: growth chart, funnel, usage meters); newest-first review fetch via the legacy Places API (new key, new Vercel env); Encore's wrong place_id found and fixed before any data landed (see FIRST TASKS item 4); Encore Entertainment onboarded as business #2; GBP Business Profile API access application submitted (7-10 day review, Open items 9); second-pass competitor audit done (docs/2026-07-12-second-pass-audit.md; both trial accounts verified, GatherUp charges $99 on Jul 25 unless canceled, reminder trigger set for Jul 23). Latest deployment dpl_6z2UdrADzmWJWbj5za7NRTRbDm6d. Only CRM Phase 6 (monthly report + archive) remains in the plan. Everything below is live and verified unless marked open.
**PATH FORWARD (2026-07-12 version, superseded by the current one above):** (1) run the FIRST TASKS checklist below NOW, in-window (the staged test fired ~9:20am PT today; the first-ever snapshot for both businesses lands at the 10am PT tick); (2) build Phase 6 (monthly report + archive, ~1 day) and fold in the three small audit items: Resend email.opened tracking into sends + portal funnel, reviews-tab replied_at + admin mark-replied action, industry benchmark line; (3) Event Mode kiosk (~half day, spec ready, docs/2026-07-12-event-mode-spec.md); (4) GBP OAuth phase once the application is approved (~Jul 19-22, quota flips 0 -> 300 QPM): full review reads + reply posting, retires the 5-review window, automates replied_at. Chris's revenue-side items in parallel: Stripe live activation, Twilio EIN brand, WER GBP video verification, and pick pilot #1 (recommendation on the table: Encore Entertainment, already onboarded at 3.5 stars / 8 reviews; a past-customer list would exercise the whole engine on his own business first).
**Continue from this doc in a new session.** Per-doc history lives in `docs/`. Site + engine source: `site/wer-site-2026-07-12p.zip` (latest deployed copy; the b zip was the Phase 4 snapshot). Chat-side context pack for the claude.ai Project: `WER-PROJECT-KNOWLEDGE.md` (condensed from this doc 2026-07-12; this doc stays canonical; it does NOT yet reflect the CRM phases 0-3, subdomain, or Phase 3 plumbing).
**Staged engine test: PASSED 2026-07-12 16:23 UTC** (ran in the prior session via its scheduled trigger). Stage 0 fired on the 16:00 UTC cron; manual backdated ticks walked TestWalk through stages 1-3 (seq_stage 4, done); TestClick clicked before stage 2 and was marked done with NO reminder send (clicker-skip verified, tick returned sends:1); post-done tick returned sends:0. Event log holds the full trail (7 send rows 16:00-16:23Z). Test rows deleted via whole-row deleteDimension per the L-N caution; contacts tab verified clean.
**FIRST TASKS [ALL DONE + VERIFIED 2026-07-12 ~10:15am PT; kept for reference, detail in wer_crm_build memory]:** (1) confirm the staged test's sequence sends produced rows in the new `sends` tab with Resend message ids (tick-path send-logging evidence; the webhook path is already verified); (2) confirm contacts rows 4-5 were cleaned whole-row, fix ghost rows if the other session range-deleted; (3) SMS send-log test, Chris-approved targets: his cell, the 619 452 0435 voip.ms line, or the DID ending 3730 (all forward to his email): add a test contact to Encore, let the tick send in-window, confirm a sends row with a Twilio SID, clean up; (4) after the 10am PT snapshot, backfill Encore's baseline_rating/baseline_count on businesses from the earliest snapshot (Phase 0 left them blank, no snapshots existed yet). CONTEXT from the overnight session: Encore's place_id was WRONG until 2026-07-12 ~1am PT; it pointed at a Round Table Pizza in Lincoln, CA. Chris confirmed the real listing is Encore Photo Booths, 6520 Lonetree Blvd, Rocklin, CA (place id ChIJyQk5FSUim4ARpxnh1MpmXO8, 5.0 rating, 2 reviews); businesses!G2 is fixed and verified. The snapshots tab was still EMPTY at fix time (no snapshot had ever run), so the first snapshot ever is today's 10am run against the correct listing; backfill from that, expect 5.0 / 2. The same tick's reviewScan should report newReviews 0 because Encore's 2 reviews are already baselined in the reviews tab; (5) confirm the tick skips a suppressed contact (bounced@resend.dev is in suppressions). Phases 4 AND 5 are DONE (overnight session; Encore Entertainment onboarded as business #2, so the 10am snapshot covers both businesses). Extra morning check (6): after the 10am snapshot and the baseline backfill, open both scoreboard links and confirm the growth chart draws from the real snapshot rows; that formally passes Phase 5's acceptance. Note the backfill now covers TWO businesses: Encore Photo Booths (expect 5.0 / 2) and Encore Entertainment (expect 3.5 / 8), set joined_at + baseline_rating + baseline_count on both. After these checks proceed to CRM Phase 6 (monthly report + archive) per docs/2026-07-12-crm-plan.md. Also pending: copy the overnight session's new scripts into Home/automation/wer/ if that session could not (it only had Home/credentials connected): phase4_migrate.py and deploy_vercel_fallback.py.

## Parallel session 2026-07-12 day/evening: SEO push, customer login, referrals, GBP agency dry run

**Ran ~10am-6pm PT Jul 12 alongside the CRM session; site zip of record: site/wer-site-2026-07-12q.zip (current deployed tree; earlier in the session the tree was verified byte-identical to production).** Shipped and verified: (1) all six FIRST TASKS from the morning header PASSED with evidence (sends rows with Resend ids, contacts clean, SMS send-log with Twilio SID, baselines backfilled from the first 10am snapshot, suppression skip, both scoreboards drawing real snapshot rows). (2) Indexing: Talaria indexing skill pushed WER pages to the Google Indexing API (project quota exhausted mid-run; remainder queued with a flush trigger for after midnight PT, 28 URLs incl. the GBP guide); 11 pages manually submitted in GSC before its daily cap; Ahrefs analytics snippet live sitewide (Chris to-do: Recheck installation in Ahrefs). (3) Competitor keyword-gap analysis cached in docs/2026-07-12-competitor-keyword-gap.md + -DATA.md; Tiers 1-4 of the resulting plan EXECUTED: 15 new pages live (comparison/alternative/pricing pages, guides, /free-reputation-report tool with reviews-to-4.5/4.8 math + email capture, /google-business-profile-setup guide), footer Guides column sitewide, comparison tables moved high with the WER row shaded trust-soft green, sitewide paragraph-spacing standard. (4) Ahrefs site audit: both exported issue reports fixed, health score 100 on recrawl; AI-content ratings driven down across flagged pages (deep structural rewrites; the two stubborn pages rebuilt by architecture change). Tier 5 (per-trade templates), link building, YouTube, and an AI-distribution recrawl remain open. (5) "No login" positioning removed sitewide; passwordless customer login LIVE and verified end to end in production: /login page, api/auth.js (request_link emails AND texts a 15-min HMAC link, verify sets a 30-day HttpOnly session, Google Sign-In config-gated on GOOGLE_OAUTH_CLIENT_ID, logout, whoami), api/_auth.js token helpers, api/dashboard.js session-to-scoreboard redirect. Chris to-dos: set GOOGLE_OAUTH_CLIENT_ID in Vercel to activate Google Sign-In; click his own emailed sign-in link as the final acceptance. (6) Scoreboard upgrades live: clickable WER logo header, "See your listing on Google" + review-link links, referral card (signup?ref=biz-id + copy button); stripe-webhook now detects ref= on converted leads and flags "credit the referrer one free month" in the payment email + logs referral_conversion (code-reviewed; fires on the first live referral). Referral terms still need a terms-page paragraph before live billing. (7) Comp accounts + tester invites live in /admin (comp checkbox writes plan=comp with mrr 0; invite action sends the welcome email + SMS; end-to-end tested and cleaned up). (8) Money-back wording standard everywhere: "$99 first month, money-back guaranteed."

**GBP agency model, dry run status (the live loose end):** runbook v2 = docs/2026-07-12-gbp-guided-setup-runbook.md (agency model: WER builds the whole listing, parks at Verify Later, transfers via Owner invite, customer only films the live verification video; video shot list included). Dry-run listing BUILT and PARKED under electricimages619@gmail.com: "Encore Event Services," 100 Amalfi Ct Lincoln CA (service-area, address hidden; category "Party equipment rental service" because "Photo booth" demanded a storefront), phone 916-248-5025, NOT PUBLICLY VISIBLE, Business Video the only verification method offered. Hours, photos, and services intentionally skipped for now. FOUND LIVE: Google's owner-invite emails are BROKEN industry-wide (since ~Jun 12 2026 per seroundtable.com/google-business-profile-invites-broken-41504.html, Product-Expert confirmed). Our evidence: invites to wellearnedreviews@gmail.com AND chris@encoresac.com both registered PENDING in People and access but neither mailbox ever received anything (in:anywhere empty past the one-hour window); the invite address was verified character-exact. WORKAROUND documented in the runbook, whose outreach template now sends customers there: the invited account accepts at myaccount.google.com/brandaccounts under Pending invitations, no email needed (the surface exists; the re-sent invite had not propagated to it yet as of the last check ~10 min after the re-add). BLOCKED ON: wellearnedreviews@gmail.com got signed out of Chrome in a session reset; Chris signs it back in, then Claude checks Pending invitations, records the accept flow up to the Accept button (Chris clicks Accept, as with all access changes), continues through the Get verified screen, stops before submitting. Plan agreed with Chris: document the workaround flow now, re-shoot the email-based version once Google fixes delivery. Recorded so far (GIFs on Chris's machine): gbp-setup-encore-event-services.gif (50-frame build), gbp-owner-invite-handoff.gif, gbp-invite-troubleshooting.gif. Related finding: chris@encoreeventservices.com BOUNCES (MX points at Google but the mailbox does not exist; fix = Workspace alias if Chris wants the address); chris@encoresac.com verified deliverable. After verification clears: finish hours/photos/services, then runbook Phase 3 (place_id, review link, contacts). Chris still records the guide-page video (shot list in the runbook incl. the People-and-access beat); then embed it on /google-business-profile-setup with VideoObject schema.

## What this business is

Done-for-you Google review generation for local businesses. Customers sign up self-serve at wellearnedreviews.com: $99 first 30 days, then $249/mo, or $174/mo billed annually (30% off = $2,088/yr, save $900; early cancel re-rates used months at $249 and refunds the remainder, discount is retroactively lost). Intro month caps at TRIAL_CAP=50 enrolled customers (waitlist auto-promotes after day 30; verified live). Ongoing GatherUp-model caps: 300 SMS + 3,000 email per business per month (MONTHLY_SMS_CAP/MONTHLY_EMAIL_CAP, per-channel enforcement in tick, verified live). Trial-model decision pending (recommendation: keep $99 intro + add 30-day money-back; see docs/2026-07-11-email-campaigns-and-trial-model.md, which is also the email-campaigns architecture input for the CRM). Competitor pricing context in docs/2026-07-11-competitor-pricing-and-trial-cap.md. Positioning: never gates reviews (everyone gets the same link), we do ~95% of the work, and the owner runs it by text or uses the simple dashboard, their choice (2026-07-12 change: absolute no-login claims removed sitewide, do NOT reintroduce them in new copy; claims that competitors require logins stay, they are true). Voice: deadpan, plainspoken, no AI tells (encore-writer rules; no em dashes anywhere, including JSON-LD).

## Live infrastructure map

- **Domain:** wellearnedreviews.com, registered at Cloudflare (Chris did it manually; API blocked at 100-domain account cap). Zone id 8ce50823c5b93d735ae97ae79b9661da. DNS: A @ 76.76.21.21, CNAME www → cname.vercel-dns.com (DNS-only), Google TXT verification, Vercel-hosted.
- **Hosting:** Vercel project `wellearnedreviews`, team `chris-terry-hermes-projects` (team_zqYDA6v9hqYOoObOfsVSPTbe), Pro plan. Static HTML + serverless functions in `api/`. Deploys via REST (file-hash upload + /v13/deployments; python pattern in session logs, no CLI needed). vercel.json: cleanUrls, /r/:token rewrite, crons.
- **Crons:** /api/drip daily 16:00 UTC (nurture emails), /api/engine-tick hourly (sequencer + daily snapshots at 17 UTC + Monday digests at 15 UTC). Both bearer-authed with CRON_SECRET.
- **Data store:** Google Sheet "WER Leads" id `139RlDzq-riJKTemFwZwHk1zHk6uPBs1G_TBYSRd4-GU`, owned by chris@encorepromo.us, edited by service account talaria-sheets@hermes-integration-499005. Tabs: `leads` (funnel), `businesses`, `contacts`, `events`, `snapshots` (engine). This sheet is currently the CRM.
- **Email (sending):** Resend Pro team "encorephotobooths" ($20/mo Transactional Pro, 50k emails/mo, renews Aug 11). wellearnedreviews.com VERIFIED on that team (domain id 91429413-cdd6-4dd2-9ca6-ab88c9c0c2ae); all site mail sends as "WellEarnedReviews <hello@wellearnedreviews.com>". Key: RESEND_WER_API_KEY in Home/credentials/resend.env (the older RESEND_ENCOREPROMO_API_KEY is a separate FREE team, no longer used by WER). Audience "WER Leads" 384ac4a7-664b-449a-adaf-f83558e55393 on the Pro team (old encorepromo-team audience f54fa1ad had only an unsubscribed test contact; not migrated). Resend DNS on Cloudflare: resend._domainkey TXT + send.wellearnedreviews.com MX/TXT (SPF). NOTE for CRM: Resend's API blocks the container's python urllib (Cloudflare 1010); use curl with a browser User-Agent.
- **Email (receiving):** wellearnedreviews.com added as Secondary Domain in Google Workspace, verified, Gmail activated. hello@wellearnedreviews.com is an alias on chris@encorepromo.us, so replies land in Chris's Gmail. DNS on Cloudflare: MX @ -> smtp.google.com pri 1, SPF "v=spf1 include:_spf.google.com ~all", DMARC p=none, Gmail DKIM google._domainkey (authentication started in admin console). Verified live: test mail to hello@ delivered, proof mail FROM hello@ delivered (Resend ids fefc1782, 8fd446be). Optional Chris to-do: add hello@ as a "Send mail as" address in his Gmail settings to reply from that identity.
- **SMS:** Twilio (Encore Photobooths account): number +19166196827, APPROVED sole-prop brand + VERIFIED campaign; fine for dev/testing/Encore's own sends, NOT for other businesses (needs standard EIN brand, open item). Inbound webhook → /api/sms-inbound (STOP handling). Public contact line: voip.ms DID **+1 619 452 0435** (San Diego, SMS-to-email → chris@encoreimages.us, routed to subaccount 181707_102); it does NOT send automated SMS (would need its own A2P).
- **Google:** GSC domain property verified (DNS TXT via service account), sitemap submitted, owners include chris@encorepromo.us + chris@encoreimages.us. Places API key = "Encore CRM" key in google-api-keys.env, referrer-allowlisted for wellearnedreviews.com (server proxy sends that referer). IndexNow key 22bb0a098910216dc5035cf930468daa (key file hosted on site).
- **Billing (Stripe, TEST MODE, built 2026-07-11):** Stripe login = Chris's corp email, structure = one account per DBA under Encore Industries LLC. WER account acct_1Ts9YZQXyFz6X97q (display name still "New business"; rename + activate pending). Catalog: wer_monthly $249/mo + coupon WER_INTRO_99 ($150 off first invoice = $99 intro), wer_annual $2,088/yr. Site flow: /signup done-step "pay now" buttons -> /api/checkout (Checkout Session, monthly auto-applies intro coupon) -> Stripe -> /welcome success page. Webhook we_...SeeI -> /api/stripe-webhook (verifies by re-fetching event id from Stripe; flips lead rows to converted; emails Chris on payment and on subscription cancellation). VERIFIED END TO END in test mode: real checkout with test card showed $99 today then $249/mo, redirected to /welcome, lead row flipped to converted, payment email sent; test sub canceled + row cleaned after. Keys/ids in Home/credentials/stripe.env. TO GO LIVE: Chris activates the account (LLC EIN + bank + statement descriptor WELLEARNEDREVIEWS, support email hello@), then recreate the catalog + webhook in live mode (same API calls, live key) and swap Vercel envs STRIPE_SECRET_KEY / STRIPE_PRICE_* / STRIPE_WEBHOOK_SECRET. The live webhook MUST subscribe to all three event types: checkout.session.completed, customer.subscription.deleted, AND invoice.payment_failed (the CRM's at_risk automation depends on the third; the test endpoint was missing it until 2026-07-12).
- **Secrets:** Home/credentials/wer-secrets.env (WER_ADMIN_SECRET for /admin, WER_CRON_SECRET, sheet/audience ids, phone). Also: twilio.env, voipms-fresh.env (VOIPMS_API_PASSWORD works; token does not), resend.env, google-api-keys.env, anthropic.env, openai.env (TTS), vercel.env, cloudflare.env (CLOUDFLARE_API_TOKEN = DNS write; CLOUDFLARE_NEW_API_TOKEN = zones/registrar read + registrar write). NEVER put values in chat or docs.

## The site (all pages live, mobile-polished, trust-green palette)

Landing (hero phone animation, 3 steps, no-gating band, 2-card pricing + live ROI calculator, FAQ), /signup (3-step wizard, real lead capture, GBP checkbox), /demo (dashboard demo), /contact (form + phone), /integrations (Zapier walkthrough), /privacy, /terms, /admin (operator console, WER_ADMIN_SECRET). Free tools: /google-review-link-generator (Places search + paste path + QR kit + email capture), /ai-review-response-generator (Claude Haiku behind /api/generate-response, guardrailed), /review-request-templates (live-personalized). Phase B content: 5 review-response-examples posts (negative/1-star/5-star/contractor/dental-HIPAA). Brand mark = star-check logo (see Brand section; /img/wer-mark.png + favicon set). FAQ now includes buying-reviews and guarantee questions (visible + FAQPage schema, deployed dpl_Hwdm7c6wfwgGXy5Abc8K2DeDY55A). Sequence copy sitewide says three asks max (index step 02, integrations, llms.txt, templates page has 4-message set); Trustpilot description updated to match. Mobile: chip quick-nav, full-width CTAs, verified at 390px. robots.txt allows AI crawlers; llms.txt current; sitemap current (28 urls); IndexNow pinged on every page addition. ADDED 2026-07-12 afternoon (see wer_seo_gap memory + docs/2026-07-12-competitor-keyword-gap.md): 14 SEO-gap pages, Tiers 1-4, including /free-reputation-report (tool=report branch in api/toolkit.js) and the comparison set; /google-business-profile-setup guide; footer gained a Guides column (grid now 4 columns) and a Customer login link. Copy standards now in force on every new page: 16px paragraph spacing rides the tool-wrap container, comparison tables sit HIGH on the page, WER row/column highlighted trust-soft green, never target buy-reviews terms. Audit recrawl: health 100, zero errors.

## The funnel (verified end to end)

Every tool email-capture and signup → row in leads tab (source, consent, UTM) + Resend audience (subscribed only with consent checkbox). Nurture drip: day 2 ROI, day 5 no-gating, day 8 offer, day 14 close; HMAC unsubscribe links + List-Unsubscribe; converted/unsubscribed drop out. Contact form feeds same system. Lead notifications email Chris.

## The engine (verified with live sends)

Onboard a business at /admin (or API): creates scoreboard magic link + webhook URL. Contacts enter via admin paste, or per-business HMAC webhook `/api/hook?b=<id>&k=<key>` (Zapier/CRM/POS payloads, flexible field mapping, dedupe, 401 on bad key). Sequencer (updated 2026-07-12): FOUR messages, dry-humor templates (approved by Chris, in api/_engine.js): check-in at 16h+, ask at +24h (click-tracked link /r/<token>), reminder at +72h and final reminder at +96h after that, both only if unclicked; deployed dpl_5hp8NfCwbeiX2EQiXNT1YCFMxPA4, render-verified all stages; staged live test PASSED 2026-07-12 (all 4 stages, clicker-skip, and done-state no-op verified with event-log evidence; test rows cleaned up); 9am-6pm PT window; 8 sends/business/hour; email always, SMS only if business sms_ok=yes. STOP → opted_out. Daily Places snapshots; Monday plain-English digest; scoreboard shows rating/count/requests/clicks. Business #1 onboarded: Encore Photo Booths (encore-photo-booths-ti109g). Full sequence verified live including click suppression of the reminder. COMP ACCOUNTS (2026-07-12 afternoon, dpl_C8Af92oFD68q4hiaRT4L6aLkcREM): /admin add-business has a Comp checkbox (plan=comp, mrr=0, joined_at=now; Stripe never touches comp rows) and a per-business `invite` action that emails the owner a welcome (SMS too if sms_ok=yes); verified end to end in production. Free-tester flow: add business with comp checked, then send invite.

## Customer login + referral (2026-07-12)

Passwordless login LIVE at /login (Chris chose passwordless over email/password; footer link sitewide). Flow: email OR cell in, api/auth.js sends a 15-minute HMAC sign-in link to BOTH channels on file (Resend + Twilio), click sets a 30-day HttpOnly cookie, /api/dashboard 302s to the business's magic-link scoreboard. No passwords stored, no new sheet tab: api/_auth.js mints stateless HMAC tokens off CRON_SECRET; anti-enumeration generic responses; auth_link/auth_login events logged. Verified in production end to end 2026-07-12 ~1:30pm PT (forged-token round trip, no-cookie 302, bad token 400, logout, real request_link fired both channels). Google Sign-In is BUILT and config-gated: goes live when Chris creates an OAuth client id and sets GOOGLE_OAUTH_CLIENT_ID in Vercel + redeploys (open item 10). Scoreboard also gained: clickable WER logo header, "see your listing on Google" + review-link lines, and the REFERRAL card ("Give a month, get a month", /signup?ref=<biz_id>): ref lands in leads.utm automatically, stripe-webhook logs referral_conversion and flags the payment email; CREDIT IS MANUAL in Stripe. Referral terms on /terms still owed before live billing (open item 11). Details: wer_customer_login memory.

## Assets

site/wer-demo-video.mp4 (55s, AI narration), site/wer-demo-SILENT-master.mp4 + narration script (Chris will record his own VO; when his audio arrives, sync + master with ffmpeg). Not yet embedded on site pending Chris's approval.

## Brand, domains, Trustpilot (added 2026-07-12)

- **New logo live.** Chris supplied logo files (project root: wellearnedreviewsLOGO-HR.png 1518x566 transparent, LR variant, favicon jpg, PSD source). The star-check mark is cropped from the HR file into site assets: /img/wer-mark.png (header + footer, footer renders it white via CSS filter), /favicon.png, /favicon.ico, /apple-touch-icon.png. Old five-star SVG and favicon.svg are gone from every page. Deployed dpl_D33XGkzAPuttVhHZ1hnmsf5svF15, verified live on all 17 pages. Site palette stays green (Chris chose logo swap only, no reskin). If logo files change again: re-crop from HR at x=546 (mark/text gap is columns 533-558), regenerate the four assets, redeploy.
- **Redirect domains live.** hardearnedreviews.com and encore-reviews.com (both registered at Cloudflare, zones active) 301 to https://wellearnedreviews.com preserving path+query, via proxied placeholder A records (192.0.2.1, @ and www) plus a dynamic-redirect ruleset per zone. Both also carry parked-domain email hardening: TXT "v=spf1 -all" and _dmarc p=reject. Verified live over http/https, apex/www, with paths and query strings. Cloudflare API access via CLOUDFLARE_FULL_ACCESS_TOKEN in Home/credentials/cloudflare.env.
- **Trustpilot LIVE** at trustpilot.com/review/wellearnedreviews.com (account email hello@, free plan, 14-day Plus trial unlocked but unused; do not buy paid tiers). Set and verified on the public page: name WellEarnedReviews, primary category Internet Marketing Service, company description (the approved 139-word deadpan one), domain, contact info (hello@, 619 452 0435, public mailing address 2220 Otay Lakes Rd Suite 502-302, 91915, entered by Chris; the Jamul registration address stays off every public surface). Remaining: Chris clicks Upload logo in Settings > Profile page > About your business and picks wellearnedreviewsLOGO-LR.png from the project root (Claude's browser bridge cannot drive native file pickers); optional: the Confirm company details section. Invitations stay OFF until real customers exist (50/mo free).

## Measurement (added 2026-07-12, late session)

- **GA4 LIVE.** Property WellEarnedReviews (id 545196739) under account Encore (128590939), web stream "WellEarnedReviews site" 15241676753, measurement id G-KJW8E3ZW0S. gtag on all 17 pages (injected before /head on every page). Verified end to end: realtime showed 2 active US users minutes after deploy. Industry Business and Industrial, objectives generate leads + understand traffic, Enhanced measurement on.
- **Vercel Web Analytics LIVE.** Chris enabled the project toggle 2026-07-12; the script route needed one redeploy after enabling (dpl_C87cdTNDu3rPAWJPMhnMBdTbphsY). Verified end to end: script.js 200 and the pageview beacon POST /_vercel/insights/view returned 200 on a real page load. Note for the future: for this static setup the insights route bakes in at deploy time, so enable first, then deploy.
- **Privacy policy** now discloses both analytics tools (plain-voice paragraph, date bumped to July 12, 2026).
- **GSC: three domain properties**, all verified via service account with chris@encorepromo.us and chris@encoreimages.us delegated as owners: wellearnedreviews.com (sitemap submitted), hardearnedreviews.com, encore-reviews.com (the redirect domains, registered for monitoring only).
- Deployment for the tagging pass: dpl_5vkBuzVNSw73LmN6hXMKwdxUuWD9.

## Google Business Profile (2026-07-12)

- **Duplicate Encore Photo Booths listing REMOVED** from the GBP account (identity double-checked before the irreversible click: the copy had an empty store code vs the keeper's 11746960, and carried Google's own "remove this copy" banner; account went from 3 businesses to 2).
- **WellEarnedReviews profile CREATED:** category Internet marketing service, service-area business covering California, registration address entered but hidden from public (service-area setup; the Jamul address stays off every public surface). **Verification pending:** Google requires the Business Video method for this profile; Chris records it at business.google.com/verify/l/05545170398901229065. Until verified the profile is not publicly visible.
- **Encore Promotional Products profile:** keep-or-remove is Chris's call, still open.
- **Agency-model setup for customers WITHOUT a profile (2026-07-12, proven live on Modern Roofers):** WER builds the ENTIRE listing under Chris's account, parks it at Verify Later (not publicly visible), transfers via People and access with role OWNER (Manager cannot complete verification), owner accepts the invite and films the verification video LIVE (it cannot be uploaded as a file), WER finishes after it clears: photos, place_id fetched and IDENTITY-VERIFIED (Round Table Pizza lesson), review link set in /admin BEFORE any contacts load. Runbook: docs/2026-07-12-gbp-guided-setup-runbook.md; customer-facing guide live at /google-business-profile-setup. Owner's total time: one invite + a 60-second video.

## Open items

**Chris:** (1) pick pilot business + customer list; (2) client SMS rails: per-client A2P model recommended 2026-07-13 and runbook drafted (docs/2026-07-13-a2p-client-sms-runbook.md: collect 5 fields at onboarding, file a sole-prop brand + campaign + local number PER CLIENT under the Twilio account, ~$24.50 one-time + ~$6.50/mo per client, file at contract signing since vetting takes ~3 days to weeks; small engine change queued: businesses.from_number + per-number STOP mapping); the old "WER standard EIN brand" framing is superseded, and voip.ms A2P matters only if the contact line ever needs to send; (3) record video narration; (4) billing rails (no Stripe yet; signup promises billing on onboarding call); (5) optional: Gmail "Send mail as" for hello@wellearnedreviews.com; (6) record the WER GBP verification video and decide keep-or-remove on the Encore Promotional Products profile (see Google Business Profile section); (7) upload the Trustpilot logo (see Trustpilot bullet); (8) paste the WER Legacy Places key from the Cloud console into Home/credentials/google-api-keys.env as GOOGLE_PLACES_LEGACY_KEY (it is already live in Vercel; this is just the file-of-record); (9) Business Profile API access application: SUBMITTED 2026-07-12 ~2am PT (Chris clicked through the guided workflow at support.google.com/business/workflow/16726127, signed in as chris@encorepromo.us, anchored on the Encore Photo Booths verified profile at 6520 Lonetree Blvd Rocklin with website encorephotobooths.com, project number 1061349459644). Google quoted 7-10 days, so expect a decision around Jul 19-22. Approval check: Business Profile API quota in the Cloud console flips from 0 to 300 QPM (also a follow-up email to chris@encorepromo.us). Once approved: enable the Business Profile APIs on hermes-integration-499005, OAuth consent + refresh token with the GBP owner account, then replace the Places 5-review window with full review read + reply posting in the CRM (Phase 4's placeReviews and the drafting path in _reviews.js are the seams to swap); (10) create a Google OAuth client id for wellearnedreviews.com and set GOOGLE_OAUTH_CLIENT_ID in Vercel + redeploy, which activates the built Google Sign-In on /login (~2 min); (11) referral-program terms paragraph on /terms before billing goes live. DONE 2026-07-12: Vercel Web Analytics enabled and verified. DONE 2026-07-11: Resend Pro + brand-domain sending + Workspace receiving (see Email sections above).
**Build queue additions from the 2026-07-12 second-pass competitor audit (docs/2026-07-12-second-pass-audit.md; both trials email-verified, GatherUp cancel-reminder set for Jul 23, trig_016XBEsJSZyJPPXzXpiNqA9w):** email-opens tracking via Resend's email.opened webhook event (sends row status + portal funnel row, ride along with Phase 6); reviews tab `replied_at` + admin mark-replied action so the digest can report reply coverage (automates once GBP OAuth lands); industry-benchmark line for the portal/monthly report (hand-set averages per industry); note for comparison-page copy: NiceJob shows nothing until the owner OAuths Google, WER's dashboard fills itself.
**HighLevel teardown DONE 2026-07-13** (docs/2026-07-13-highlevel-teardown.md; read-only, nothing connected, trial clean for a second pass): Chris's trial is Agency Pro **$497/mo and the card auto-charges when the trial ends ~Aug 10**; cancel-or-decide reminder set for Aug 3 9am PT (trig_01Te5PwYCYXvbbzsLo2szWza, push+email). Headlines: their cadence is three dropdowns (send-after-check-in, repeat-until-CLICKED, max retries), click-exit only, every channel OFF by default, default sender is the signup Gmail, so WER's review-detection exit + done-for-you onboarding beat the $497 platform's defaults; per-sub-account A2P Trust Center wizard confirms per-client sole-prop brand registration is the industry-standard model for client SMS (the runbook answer to open item 2); GBP module and Reviews AI are dead until owner OAuth (same comparison-page ammo as NiceJob). Steals queued in the doc: "unlock N reviews" onboarding tease from Places data, AI-reply wait-time + backlog drip mode + per-rating tone for the GBP OAuth phase, 3-competitor rating/sentiment panel for the monthly report, Prospecting-style outbound audit reports (their $29/mo module = WER's review-gap-checker aimed at prospects). Verdict: do not build on HighLevel; take the patterns, keep the stack.
**Build queue:** Event Mode kiosk capture (SPEC READY + acceptance test confirmed by Chris 2026-07-12: live kiosk submit creates a consented source=kiosk row, thank-you resets, no review link on page, check-in fires next send window; see docs/2026-07-12-event-mode-spec.md, ~half-day); review monitoring alerts SHIPPED 2026-07-12 as CRM Phase 4; GBP OAuth reply posting is now CRM Phase 7 in the plan doc (with reply delay + drip + approval routing); Phase C cornerstone guides ("how to get more google reviews", GBP mega-guide), Phase D comparison pages (birdeye/podium alternative, KD 0-15), review gap checker tool, AI share-of-voice baseline (Talaria ai-visibility workflow).

## NEXT PROJECT: the CRM (decided 2026-07-11)

Goal: manage the sales pipeline AND give customers an easy way to track results.
Current de facto CRM = the WER Leads sheet + magic-link scoreboards. The CRM should grow from these, not replace them blindly.
Two faces: (1) **Operator pipeline** for Chris: leads → contacted → onboarding → active → at-risk → churned, with per-account revenue, source attribution (UTM/tool data already captured), and tasks; (2) **Customer results portal**: today's scoreboard evolved: review growth chart from snapshots, request/click/review funnel, monthly report archive, competitor tracking, all behind the existing passwordless magic links.
DECIDED 2026-07-12 (Chris): storage stays on Google Sheets; the operator UI extends /admin (not a separate app); acceptance test = a working pipeline view where stage moves persist across reloads. SCOPED 2026-07-12 (Chris, four answers) and PHASED PLAN READY: **docs/2026-07-12-crm-plan.md** (6 phases, ~5.5 build days, per-phase acceptance tests; next action = Phase 0 sheet migration). The answers: Stripe webhook auto-moves stages (paid → onboarding, payment failure → at_risk, cancel → churned) with MRR on the board; portal v1 = scoreboard evolution (since-joining growth chart, per-step funnel, usage meters, empty-state coaching; report archive and competitor tracking wait); review-detection exit rule + 180-day re-enrollment cooldown fold in as Phase 4 (which also delivers the build-queue review-monitoring alerts); tasks = one next-action per account with a Today list. PHASES 0+1 SHIPPED 2026-07-12 (late session, deployment dpl_G5hHkyBrbWcCPsDY8ZYf9Tnaxagy): sheet migrated (CRM columns added to leads M-R / businesses L-S / contacts L-N, stage backfilled on all 4 lead rows, first tab renamed "Untitled" -> "leads"; safe because all engine reads of that tab are positional; migration verified pre-existing cells byte-identical, PASS). Pipeline board LIVE in /admin: Today list, stage-grouped cards (lead/contacted/onboarding/active/at_risk/churned/lost), tap-to-move, inline next-action editing; new engine-admin actions pipeline/move_stage/set_next, stale-guarded (wrong email = 409, verified). Acceptance test PASSED live: moved row 4 lead -> lost, fresh pipeline fetch returned lost with stage_at stamped, stage_move event row logged 2026-07-12T06:55Z, existing list/add actions regression-checked. Encore card is active with next_action set. PHASE 2 SHIPPED same night (deployment dpl_DycnYnm4V2W8bq3iy5eigada9iDx): stripe-webhook now drives stages: checkout completed -> onboarding (guarded, never demotes) + stamps linked business with stripe_customer_id/sub_id/plan/mrr (verified: monthly/249 from the real 7-11 checkout event), invoice.payment_failed -> at_risk, subscription deleted -> churned. All three transitions verified with REAL test-mode events against production, at_risk via Stripe's own auto-delivery (~5s after the failed invoice); audit rows in the events tab ("... (stripe)" suffix); all test rows + the throwaway business + Stripe test customer cleaned up after, sheet asserted back to pre-test state. FOUND AND FIXED during verification: the test webhook endpoint we_...SeeI was only subscribed to checkout + deletion, so payment_failed never delivered; enabled_events now includes invoice.payment_failed (go-live checklist updated to match). CRM SUBDOMAIN LIVE same night: crm.wellearnedreviews.com (Cloudflare CNAME crm -> cname.vercel-dns.com DNS-only; domain attached to the Vercel project) serves the operator console: / 307s to /admin, every other crm path 307s to the main domain (admin, api/, styles, favicons, img excluded), and wellearnedreviews.com/admin 307s to crm.wellearnedreviews.com/admin so the console has one home; verified live end to end including pipeline API on the crm host and no redirect loops (deployment dpl_DGgCBDc9YuWJ7fTKaxHWbzGQYjLn, then dpl_DycnYnm4V2W8bq3iy5eigada9iDx). Build scripts now in Home/automation/wer/ (phase0_migrate.py, deploy_vercel.py, verify_phase1.sh; moved 2026-07-12 once the folder was connected). PHASE 3 SHIPPED same night (deployment dpl_C4R49ecp5LKMRS4bgB7e61jswKiW): sends tab (ts, biz_id, contact, campaign, channel, message_id, status) written on every successful email/SMS send by tick, drip, and digest (resendSend now returns the Resend id, smsSend returns the Twilio SID); suppressions tab (business or *, identifier, reason, ts, source) checked before every sequencer and drip send, 1 legacy unsubscribe migrated in; contacts gain undeliverable as a terminal status the tick skips. New /api/resend-webhook (Resend webhook id 3836ebb8-ffea-40f8-86cd-c75937e087c6, events delivered/bounced/complained) authenticates like the Stripe webhook: it re-fetches the email by id from Resend and acts on the TRUE last_event, so no shared secret and forged payloads are useless. Hard bounce -> sends row bounced + suppression + sequence contact marked undeliverable; complaint -> suppression + contact opted_out. VERIFIED LIVE 2026-07-12 late session: real sends to bounced@resend.dev and delivered@resend.dev round-tripped through Resend's real webhook in ~5s (sends statuses flipped correctly, suppression written for the bounce only, email_bounced event logged, malformed ids 400). Still pending in-window: tick-path send-logging + suppression skip + an SMS send-log test (see FIRST TASKS in the header; the next session owns them, the old 9:45am timer was deleted). PHASE 4 SHIPPED 2026-07-12 overnight (~1am PT, deployment dpl_7kz4CGHiu1NhyGVikdeKGUBCAJVX): review detection lives in new api/_reviews.js, rides the daily 17 UTC snapshot block in engine-tick, plus a new admin action `review_scan` (optional biz_id) for on-demand scans. New `reviews` tab (ts, biz_id, review_id, author, rating, published, text, matched_contact, kind) is the last-seen store + audit; a business's FIRST scan ingests everything as kind=baseline with no exits and no alerts (a no-review business gets a baseline-empty marker row), so onboarding never alerts on old reviews. Exit rule: conservative fuzzy first-name match (normalized exact or prefix, both sides 3+ chars; Mike does not match Michael), review must postdate enrollment (last_enrolled_at || added), exactly one candidate required (2+ logs review_ambiguous, no exit); a match sets contacts status done + reviewed_at, logs review_matched. Alerts: rating 3 or less emails the owner (and TO_ADDR when different) with the review plus a draft from /api/generate-response, SMS to owner only if sms_ok=yes, logged in sends as campaign=alert; 4+ rolls into the Monday digest, which now lists the week's new reviews by name and stars. Cooldown: /api/hook and admin add_contacts dedupe became re-enrollment: opted_out/undeliverable NEVER re-enroll ("suppressed"), in-sequence returns "already enrolled", done within cooldown_days (businesses col S, blank = 180) returns "in cooldown", done past cooldown re-enrolls IN PLACE (added/last_enrolled_at = now, stage 0, queued, click token + clicked_at + reviewed_at cleared, re_enrolled event); admin add_contacts also finally dedupes and reports queued/waitlisted/reenrolled/skipped, and new contact appends stamp last_enrolled_at. ALL VERIFIED LIVE against production: baseline scan of Encore's real listing (2 reviews, both ingested as baseline, zero alerts); exit rule end to end (seeded contact "Joseph" enrolled 2016-10-01, deleted the Joseph Hernandez review row, rescan matched it: status active -> done, reviewed_at stamped, review_matched event, matched_contact written); all four cooldown branches via real /api/hook POSTs (re-enrolled / already enrolled / in cooldown / suppressed); alert end to end with a throwaway business pointed at a listing carrying a real recent 1-star review (rescan after row-delete fired one alert: sends row campaign=alert with Resend id 5299e1b4, flipped to delivered by the Phase 3 webhook, review_alert event, and the fetched email contains the full review text plus the generate-response draft); idempotency (full rescan after cleanup: all zeros). All test rows cleaned whole-row; the re-detected Joseph review row was neutralized back to kind=baseline with matched_contact cleared so the daily scan does not re-count it. fuzzy matcher unit-tested 10/10 (Sam=Samantha, Jose=José, Mike!=Michael, Jo!=Joseph). NEWEST-SORT FIX, same overnight session (~2am PT, deployments dpl_BHyqsHS7xRQa3w6VbR6h2Yfk9h5i then dpl_3JVKbzaccFZMUc7M4RwKq9XfBM6C): the Places LEGACY API is now ENABLED on hermes-integration-499005 (done in the Cloud console via Chris's browser; the SA lacks serviceusage perms). Legacy web services reject referer-restricted keys, so a NEW key "WER Legacy Places" exists in that project restricted to exactly one API (Places API legacy), no application restriction; it lives in the Vercel env GOOGLE_PLACES_LEGACY_KEY (Sensitive, Production+Preview; entered clipboard-to-dashboard, the value never touched the session; CHRIS TO-DO: paste it from the console's Show key into Home/credentials/google-api-keys.env as GOOGLE_PLACES_LEGACY_KEY). placeReviews now tries legacy reviews_sort=newest with that key and falls back to v1 relevance; scan output reports per-business source (verified live: sources shows legacy-newest). FOUND AND FIXED during verification: legacy and v1 assign different review ids to the same review, so the first legacy scan re-ingested Encore's 2 baseline reviews as new; dedupe now keys on publish-time truncated to seconds + normalized author (source-independent), the 2 duplicate rows were deleted, and back-to-back production scans return 0 new (idempotent). Remaining known limit: still a 5-review window per scan (now the 5 NEWEST); GBP OAuth on the build queue is the complete fix and Chris approved queueing its Business Profile API access application (see Open items). PHASE 5 SHIPPED, same overnight session (~3am PT, deployments dpl_GBNbub2C2vukHcPPhL5FPzbvNxPD then dpl_6z2UdrADzmWJWbj5za7NRTRbDm6d): api/scoreboard.js is now portal v1. Same magic link and 403-on-bad-token (regression-checked), page renders fresh from the sheet on every load, so data lands without redeploys. Four additions per the plan: (1) "Reviews since you joined" growth chart, inline SVG line+area from the snapshots tab with biz.baseline_count anchoring the left edge at joined_at ("You joined with N reviews. Everything above that line is since."); (2) "How requests turn into reviews" per-touch funnel: Check-in / The ask / Reminder / Final nudge counts parsed from send events' "stage N" detail, plus Clicked (contacts.clicked_at) and Left a review, which counts review_matched events ONLY for identifiers still present in contacts so cleaned-up test rows drop out (found live: the deleted Joseph test match showed as 1 until this filter; now 0); (3) monthly usage meters vs MONTHLY_EMAIL_CAP/MONTHLY_SMS_CAP from the sends log, campaign=sequence, with an SMS-off note when sms_ok is not yes; (4) deadpan empty-state coaching everywhere a new business would see blanks. Chart follows the dataviz method: single trust-green hue validated against the surface (all checks pass), 2px line, 10% area wash, 4px dots with 2px surface rings, hairline gridlines, endpoint label only, tooltips on points/bars/meters, no legend (single series). VERIFIED LIVE: both business scoreboards render all sections behind their magic links, wrong token 403s, screenshot audit on the Photo Booths page (tiles, coaching, funnel with real send counts, meters all render cleanly at desktop width). ACCEPTANCE (chart built from real snapshot history) completes after today's 10am snapshot + baseline backfill; the funnel with real counts is already live. BUSINESS #2 ONBOARDED same session: Encore Entertainment (encore-entertainment-ti20v5), Chris's other Rocklin business at the same address, place id ChIJyQk5FSUim4ARLAPOeMWAJ_8 (3.5 rating, 8 reviews, go-encore.com), owner_email chris@encoreimages.us, sms_ok no, industry events, review_link = its writereview URL; baseline scan ingested its 5 newest via legacy-newest with zero alerts (two old 1-stars correctly silent as baseline). Its snapshots start at the same 10am tick. NEXT: Phase 6 (monthly report + archive) per the plan, then Event Mode / GBP OAuth from the build queue. The scoreboard token scheme (HMAC per business) and sheet tabs are the foundation either way. Competitive input: docs/2026-07-12-gatherup-teardown.md, now a FULL app audit (every section, ~30 screens, plus 18 onboarding screenshots in gatherup.com screenshots/). Headlines: their NPS-gate flow carries an FTC hedge in its own UI copy (the thing WER markets against); their Business Dashboard URL is /agency, confirming the agency-shaped seat WER occupies; Success Report frames all numbers as growth since joining (steal for digest + monthly report); integration story is one BCC address (WER's forward-the-invoice already matches it). Portal steals: review-history backfill at onboarding, visible usage meters, empty-state coaching, since-joining framing, per-business AI reply prompts. Feature candidates logged in the teardown: free review-audit lead magnet (their Review Defense, as a WER free tool), keyword SMS enrollment (the no-tablet Event Mode, pairs with /api/sms-inbound), Google Q&A monitoring, repeat-customer cooldown, social auto-posting of 5-star reviews, and the widget/badge/popup family (confirmed gap; NiceJob ships the full family). **NiceJob teardown DONE 2026-07-12** (docs/2026-07-12-nicejob-teardown.md, full app + settings audit on a clean 14-day trial, read-only). Headlines: their default campaign is 4 touches (SMS day 0 carrying the review link, then 3 emails at +2/+5/+5 days), independently validating WER's 4-message depth while WER's ungated 16h check-in stays the differentiator; exit rule is review DETECTION (stop the sequence when the review appears), smarter than click-based stop and the top CRM steal; repeat enrollment allowed with a 180-day cooldown (adopt in the CRM data model over permanent dedupe); they gate too but opt-in only (NPS "feedback routing" OFF by default, threshold 7 and below goes private), so the no-gating positioning holds against both major self-serve tools; their check-in kiosk page (nicejob.com/slug/check-in, 60-minute default delay) has zero consent language, making WER's Event Mode spec both more compliant and better positioned; reactivation ("Get Repeats," N months after last service) is their $125/mo Pro upsell, validating WER's plan to hold reactivation for regular-rate customers; per-source QR attribution (scans, clicks, reviews, avg rating) is worth copying into Event Mode's scoreboard counts; every business gets a review.new vanity short link (cheap steal for print collateral). Full copy/reject/already-ahead lists in the doc. Both trials stay clean for second passes (GatherUp ~13 days left, NiceJob 14; no sends or connects made on either).

## Session ops notes (for the next session)

Deploy = python REST pattern (in every session log; ~30s). Headless Chromium in the container cannot reach external sites through the proxy: test mobile/visuals by curling pages to a temp dir and serving on localhost. The wer-secrets.env staging pattern retrieves admin/cron secrets. Sheet writes go through the service account. One rule that bit us twice: never let a `.bar`-style class override `.wrap` padding on the same element, and always screenshot-audit after CSS changes.
